# Only needed when the frontend and backend are deployed separately (e.g. Vercel).
# Leave this unset for local development — Vite's dev proxy handles it automatically.
VITE_API_URL=https://your-backend-url.vercel.app/api
